<Window x:Class="TallyDbLoader.Wpf.CompanySelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Tally Company" Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#121212" Foreground="#FFFFFF"
        BorderBrush="#333333" BorderThickness="1"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Multiple active companies detected. Please select the company you want to configure:" 
                   TextWrapping="Wrap" Margin="0,0,0,15" Foreground="#CCCCCC" FontSize="12"/>

        <ListBox Grid.Row="1" x:Name="CompaniesListBox" 
                 Background="#1E1E1E" Foreground="White" BorderBrush="#333333" BorderThickness="1"
                 Margin="0,0,0,15">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2D2D2D"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#4A90E2"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" FontSize="13" VerticalAlignment="Center"/>
                        <Border Margin="10,0,0,0" Padding="6,2" CornerRadius="4" Background="#7B1FA2" 
                                Visibility="{Binding IsGroup, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"
                                VerticalAlignment="Center">
                            <TextBlock Text="Consolidated" FontSize="9" Foreground="White" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="CancelButton_Click" Background="#555555"/>
            <Button Content="Select Company" Click="SelectButton_Click" Width="130"/>
        </StackPanel>
    </Grid>
</Window>
